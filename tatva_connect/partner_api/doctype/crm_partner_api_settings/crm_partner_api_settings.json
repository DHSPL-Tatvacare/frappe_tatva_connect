{
 "actions": [],
 "allow_rename": 1,
 "creation": "2026-06-23 00:00:00.000000",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "section_break_rate",
  "window_seconds",
  "per_token_rate",
  "per_token_burst",
  "global_rate",
  "global_burst",
  "section_break_volume",
  "records_window_seconds",
  "per_token_read_records",
  "global_read_records",
  "per_token_write_records",
  "global_write_records",
  "section_break_payload",
  "bulk_max_records",
  "list_max_page",
  "list_default_page",
  "file_download_timeout_seconds",
  "file_download_max_mb",
  "idempotency_window_hours"
 ],
 "fields": [
  {"fieldname": "section_break_rate", "fieldtype": "Section Break", "label": "Rate limits (calls)"},
  {"fieldname": "window_seconds", "fieldtype": "Int", "default": "60", "label": "Window (seconds)", "non_negative": 1, "description": "Refill window for the CALL-rate buckets."},
  {"fieldname": "per_token_rate", "fieldtype": "Int", "default": "120", "label": "Per-token Rate (calls/window)", "non_negative": 1, "description": "Sustained API CALLS per window for ONE partner (single or bulk = 1 call each). 0 = unlimited."},
  {"fieldname": "per_token_burst", "fieldtype": "Int", "default": "240", "label": "Per-token Burst (calls)", "non_negative": 1, "description": "Per-partner call-bucket capacity (max instantaneous burst). A capacity, not a dimension: it is clamped to at least the rate, and 0 falls back to the default (240) rather than meaning unlimited."},
  {"fieldname": "global_rate", "fieldtype": "Int", "default": "300", "label": "Global Rate (calls/window)", "non_negative": 1, "description": "Sustained API CALLS per window across ALL partners combined. 0 = unlimited."},
  {"fieldname": "global_burst", "fieldtype": "Int", "default": "600", "label": "Global Burst (calls)", "non_negative": 1, "description": "Global call-bucket capacity (max instantaneous burst across all partners). A capacity, not a dimension: it is clamped to at least the rate, and 0 falls back to the default (600) rather than meaning unlimited."},
  {"fieldname": "section_break_volume", "fieldtype": "Section Break", "label": "Volume limits (rows/day)", "description": "Row (record) budgets — single + bulk combined, read and write metered separately. Cost = rows touched. Guards against cheap bulk scraping while letting real bulk loads run."},
  {"fieldname": "records_window_seconds", "fieldtype": "Int", "default": "86400", "label": "Volume Window (seconds)", "non_negative": 1, "description": "Refill window for the row (volume) buckets. Blank = code default (86400 = 1 day)."},
  {"fieldname": "per_token_read_records", "fieldtype": "Int", "default": "10000", "label": "Per-token Read Rows/window", "non_negative": 1, "description": "Rows a partner may READ per window (single + bulk combined). 0 = unlimited."},
  {"fieldname": "global_read_records", "fieldtype": "Int", "default": "50000", "label": "Global Read Rows/window", "non_negative": 1, "description": "Rows READ per window across ALL partners combined. 0 = unlimited."},
  {"fieldname": "per_token_write_records", "fieldtype": "Int", "default": "10000", "label": "Per-token Write Rows/window", "non_negative": 1, "description": "Rows a partner may WRITE per window (single + bulk combined). 0 = unlimited."},
  {"fieldname": "global_write_records", "fieldtype": "Int", "default": "50000", "label": "Global Write Rows/window", "non_negative": 1, "description": "Rows WRITTEN per window across ALL partners combined. 0 = unlimited."},
  {"fieldname": "section_break_payload", "fieldtype": "Section Break", "label": "Payload & fetch"},
  {"fieldname": "bulk_max_records", "fieldtype": "Int", "default": "100", "label": "Bulk Max Records", "non_negative": 1, "description": "Max records per bulk write call. 0 is rejected (an unbounded bulk is a footgun)."},
  {"fieldname": "list_max_page", "fieldtype": "Int", "default": "200", "label": "List Max Page", "non_negative": 1, "description": "Max records returned per read/list call. 0 is rejected."},
  {"fieldname": "list_default_page", "fieldtype": "Int", "default": "20", "label": "List Default Page", "non_negative": 1, "description": "Page size when the caller omits `limit`. 0 is rejected."},
  {"fieldname": "file_download_timeout_seconds", "fieldtype": "Int", "default": "30", "label": "File Download Timeout (seconds)", "non_negative": 1, "description": "Timeout when re-hosting an attachment from a partner-supplied source URL. 0 is rejected."},
  {"fieldname": "file_download_max_mb", "fieldtype": "Int", "default": "25", "label": "File Download Max Size (MB)", "non_negative": 1, "description": "Max size (MB) of an attachment re-hosted from a partner-supplied source URL; a larger download is rejected. 0 is rejected."},
  {"fieldname": "idempotency_window_hours", "fieldtype": "Int", "default": "24", "label": "Idempotency Window (hours)", "non_negative": 1, "description": "How long a stored Idempotency-Key + response is honoured for retry replay before the daily purge drops it. Opt-in per request."}
 ],
 "index_web_pages_for_search": 1,
 "issingle": 1,
 "links": [],
 "modified": "2026-06-23 00:00:00.000000",
 "modified_by": "Administrator",
 "module": "Partner API",
 "name": "CRM Partner API Settings",
 "owner": "Administrator",
 "permissions": [
  {"create": 1, "delete": 1, "email": 1, "print": 1, "read": 1, "role": "System Manager", "share": 1, "write": 1}
 ],
 "row_format": "Dynamic",
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": []
}
