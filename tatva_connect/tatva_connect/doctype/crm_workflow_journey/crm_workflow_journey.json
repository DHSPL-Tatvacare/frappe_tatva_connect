{
 "actions": [],
 "allow_rename": 0,
 "creation": "2026-07-20 00:00:00.000000",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "workflow",
  "workflow_version",
  "subject_doctype",
  "subject_name",
  "trigger_doctype",
  "trigger_name",
  "current_node",
  "state_json",
  "status",
  "stop_reason",
  "resume_at",
  "awaiting_signal",
  "awaiting_correlation",
  "retry_count",
  "active_key"
 ],
 "fields": [
  {
   "fieldname": "workflow",
   "fieldtype": "Link",
   "label": "Workflow",
   "options": "CRM Workflow",
   "reqd": 1,
   "read_only": 1,
   "in_list_view": 1,
   "search_index": 1,
   "description": "Records the workflow this execution is bound to."
  },
  {
   "fieldname": "workflow_version",
   "fieldtype": "Link",
   "label": "Workflow Version",
   "options": "CRM Workflow Version",
   "reqd": 1,
   "read_only": 1,
   "search_index": 1,
   "description": "Records the exact frozen version this execution runs. Pinned at start; the interpreter reads its graph, never the editable Definition."
  },
  {
   "fieldname": "subject_doctype",
   "fieldtype": "Data",
   "label": "Subject DocType",
   "reqd": 1,
   "read_only": 1,
   "description": "Identifies the doctype of the record this journey is about - half of its journey key."
  },
  {
   "fieldname": "subject_name",
   "fieldtype": "Dynamic Link",
   "label": "Subject Name",
   "options": "subject_doctype",
   "reqd": 1,
   "read_only": 1,
   "in_list_view": 1,
   "search_index": 1,
   "description": "Identifies the record this journey is about - the other half of its journey key."
  },
  {
   "fieldname": "current_node",
   "fieldtype": "Data",
   "label": "Current Node",
   "read_only": 1,
   "in_list_view": 1,
   "description": "Stores the node to run next; on resume the interpreter re-enters here."
  },
  {
   "fieldname": "state_json",
   "fieldtype": "Long Text",
   "label": "State (JSON)",
   "read_only": 1,
   "description": "Holds the single authoritative context every node reads and writes; a consumed signal's payload is merged here. Never indexed - promote a field to a column to query it."
  },
  {
   "fieldname": "status",
   "fieldtype": "Select",
   "label": "Status",
   "options": "Running\nParked\nDone\nFailed\nStopped",
   "default": "Running",
   "read_only": 1,
   "in_list_view": 1,
   "search_index": 1,
   "description": "Records whether the execution is Running, Parked, Done, Failed, or Stopped. Stopped is terminal and deliberate - the subject left, so the journey was ended rather than allowed to fail."
  },
  {
   "fieldname": "stop_reason",
   "fieldtype": "Data",
   "label": "Stop Reason",
   "read_only": 1,
   "depends_on": "eval:doc.status == 'Stopped'",
   "description": "Records why a Stopped journey was ended, so a terminal row explains itself without a reader inferring it from timestamps."
  },
  {
   "fieldname": "resume_at",
   "fieldtype": "Datetime",
   "label": "Resume At",
   "read_only": 1,
   "search_index": 1,
   "description": "Records when a clock-parked execution becomes due; the timer sweep wakes it at or after this."
  },
  {
   "fieldname": "awaiting_signal",
   "fieldtype": "Data",
   "label": "Awaiting Event",
   "read_only": 1,
   "search_index": 1,
   "description": "Records the signal name a parked execution waits for; a matching delivery wakes it."
  },
  {
   "fieldname": "awaiting_correlation",
   "fieldtype": "Data",
   "label": "Awaiting Correlation",
   "read_only": 1,
   "description": "Records the per-iteration token a wait matches a signal on, so a reused signal name cannot wake the wrong loop iteration."
  },
  {
   "fieldname": "retry_count",
   "fieldtype": "Int",
   "label": "Retry Count",
   "read_only": 1,
   "default": "0",
   "description": "Counts transient-failure retries, so a repeatedly deadlocking execution eventually fails instead of retrying forever."
  },
  {
   "fieldname": "active_key",
   "fieldtype": "Data",
   "label": "Active Key",
   "read_only": 1,
   "description": "Holds a uniqueness key while live and NULL once terminal, so the database itself rejects a second live Journey for the same subject and workflow.",
   "unique": 1
  },
  {
   "fieldname": "trigger_doctype",
   "fieldtype": "Data",
   "label": "Trigger DocType",
   "read_only": 1,
   "description": "The record whose save started this run - a Task, a File, or the lead itself. The SUBJECT is always the parent lead; this is what actually fired, and it is what a node configured to act on the trigger doc must be given."
  },
  {
   "fieldname": "trigger_name",
   "fieldtype": "Dynamic Link",
   "label": "Trigger Name",
   "options": "trigger_doctype",
   "read_only": 1,
   "description": "The record whose save started this run. Blank on a run started by the lead itself."
  }
 ],
 "index_web_pages_for_search": 1,
 "istable": 0,
 "links": [],
 "modified": "2026-07-20 19:30:00.000000",
 "modified_by": "Administrator",
 "module": "Tatva Connect",
 "name": "CRM Workflow Journey",
 "owner": "Administrator",
 "permissions": [
  {
   "role": "System Manager",
   "read": 1,
   "write": 1,
   "create": 1,
   "delete": 1,
   "report": 1,
   "share": 1
  },
  {
   "role": "Automation Manager",
   "read": 1,
   "write": 1,
   "create": 1,
   "delete": 1,
   "report": 1,
   "share": 1
  },
  {
   "role": "Sales Manager",
   "read": 1,
   "write": 0,
   "create": 0,
   "delete": 0,
   "report": 1,
   "share": 1
  }
 ],
 "row_format": "Dynamic",
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": []
}
