{
 "actions": [],
 "allow_rename": 1,
 "autoname": "field:workflow_name",
 "creation": "2026-07-15 00:00:00.000000",
 "description": "A per-subject workflow graph: typed Nodes (Step / Branch / Assign / Wait / Terminal) + edges. Authored as a Draft on the Campaigns canvas, then advanced through a lifecycle: Publish freezes an immutable CRM Workflow Version; Activate arms the entry trigger; Suspend disarms it; Archive retires it. A running Instance binds to a Version, never to this editable Definition. Born dormant (Draft).",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "workflow_name",
  "lifecycle_state",
  "grain_section",
  "vertical",
  "group",
  "program",
  "entry_section",
  "entry_doctype",
  "entry_event",
  "when_section",
  "criteria",
  "nodes_section",
  "entry_node",
  "nodes",
  "canvas_json"
 ],
 "fields": [
  {
   "fieldname": "workflow_name",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Workflow Name",
   "reqd": 1,
   "unique": 1,
   "description": "Names the workflow for operators; every Instance and log references it by this name."
  },
  {
   "default": "Draft",
   "fieldname": "lifecycle_state",
   "fieldtype": "Select",
   "options": "Draft\nPublished\nActive\nSuspended\nArchived",
   "in_list_view": 1,
   "in_standard_filter": 1,
   "read_only": 1,
   "label": "Lifecycle State",
   "description": "The Definition's editorial lifecycle, advanced only through its transition verbs (never edited by hand): Draft (authoring, mutable) -> Publish freezes an immutable Version -> Activate arms the entry trigger -> Suspend disarms it -> Archive retires it. A new Instance starts only while Active; running Instances stay pinned to their Version across every later transition."
  },
  {
   "fieldname": "grain_section",
   "fieldtype": "Section Break",
   "label": "Grain",
   "description": "The grain a subject must match for this Flow to act is set here first. A Flow is evaluated only for a record whose lead grain (vertical / group / program) matches; a blank axis is a wildcard, so an empty grain matches every subject."
  },
  {
   "fieldname": "vertical",
   "fieldtype": "Link",
   "label": "Vertical",
   "options": "CRM Vertical",
   "description": "Scopes the subjects that may enter; an Instance is created only when the subject's lead grain matches. Blank axis = wildcard."
  },
  {
   "fieldname": "group",
   "fieldtype": "Link",
   "label": "Group",
   "options": "CRM Group",
   "description": "Scopes the subjects that may enter; blank = wildcard for this axis."
  },
  {
   "fieldname": "program",
   "fieldtype": "Link",
   "label": "Program",
   "options": "CRM Program",
   "description": "Scopes the subjects that may enter; blank = wildcard for this axis."
  },
  {
   "fieldname": "entry_section",
   "fieldtype": "Section Break",
   "label": "Trigger",
   "description": "1. Trigger. The doctype and lifecycle event that start the Flow are chosen here \u2014 for example a CRM Task, on Update. The Flow is evaluated only for records of the chosen doctype and event."
  },
  {
   "fieldname": "entry_doctype",
   "fieldtype": "Link",
   "label": "Entry DocType",
   "options": "DocType",
   "description": "Declares the doctype whose lifecycle event starts an Instance; it must be a resolvable subject."
  },
  {
   "default": "Created",
   "fieldname": "entry_event",
   "fieldtype": "Select",
   "label": "Entry Event",
   "options": "Created\nUpdated\nDeleted",
   "description": "Declares which lifecycle event - Created, Updated, or Deleted - starts an Instance."
  },
  {
   "fieldname": "when_section",
   "fieldtype": "Section Break",
   "label": "When (Criteria)",
   "description": "2. When. The criteria that must all hold for the Flow to act are composed here, over the allowlisted fields \u2014 for example outcome is \"Physical Visit\" and status changed to Done. Rows are combined with AND; an empty predicate matches every trigger of the chosen doctype and event."
  },
  {
   "fieldname": "criteria",
   "fieldtype": "Table",
   "label": "Criteria",
   "options": "CRM Automation Criterion",
   "description": "The predicate that must hold for the Flow to act, composed from the allowlisted fields with the word operators (AND across rows). An empty predicate matches every trigger of the chosen doctype and event."
  },
  {
   "fieldname": "nodes_section",
   "fieldtype": "Section Break",
   "label": "Then (Actions)",
   "description": "3. Then. The actions are arranged here as an ordered graph of nodes. A Step runs an Action Group; a guard action within it (Require Fields / Require Location) is enforced at save time and can block the save, while every other action runs after. A Branch routes on a condition; a Wait parks the Flow until a timer or an event, turning it into a journey that resumes. A wait-free Flow runs once and persists nothing (a rule); a Flow that waits carries a durable instance."
  },
  {
   "fieldname": "entry_node",
   "fieldtype": "Data",
   "label": "Entry Node",
   "description": "The Node ID where a new Instance starts - the Start node the canvas wires. Set by the canvas, validated at Publish (must name a real node). A legacy Version frozen before this field falls back to the first node."
  },
  {
   "fieldname": "nodes",
   "fieldtype": "Table",
   "label": "Nodes",
   "options": "CRM Workflow Node",
   "description": "The graph: one row per node. Set each row's type and its handful of type-specific fields; edges reference other rows by Node ID."
  },
  {
   "fieldname": "canvas_json",
   "fieldtype": "Long Text",
   "label": "Canvas Layout (JSON)",
   "hidden": 1,
   "no_copy": 1,
   "description": "UI-only: node positions and viewport for the Campaigns canvas. Not read by the version freeze, so moving a node never mints a new Version; layout is presentation, the node graph is content."
  }
 ],
 "grid_page_length": 50,
 "index_web_pages_for_search": 1,
 "links": [],
 "modified": "2026-07-16 10:00:00.000000",
 "modified_by": "Administrator",
 "module": "Tatva Connect",
 "name": "CRM Workflow Definition",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  },
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "Automation Manager",
   "share": 1,
   "write": 1
  },
  {
   "read": 1,
   "role": "Sales Manager"
  }
 ],
 "row_format": "Dynamic",
 "show_title_field_in_link": 1,
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": [],
 "title_field": "workflow_name",
 "track_changes": 1
}
