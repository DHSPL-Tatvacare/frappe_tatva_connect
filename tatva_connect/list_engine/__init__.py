# The list engine: derived fields — columns a rep sees, filters and sorts that are never stored.
# Importing this package populates the registry, so a declaration lands in exactly one place.
from tatva_connect.list_engine import fields

__all__ = ["fields"]
