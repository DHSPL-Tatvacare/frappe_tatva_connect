[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations
# Register modules.txt -> Module Def FIRST, before any doctype sync (migrate on an
# existing site doesn't; add_module_defs runs only at install-app). Idempotent.
tatva_connect.patches.register_modules

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
tatva_connect.patches.add_whatsapp_message_id_unique_index
tatva_connect.patches.add_acefone_telephony_medium
tatva_connect.patches.seed_india_cities
# B5: shared-number fix — replace the global message_id unique index with a composite
# (message_id, reference_name) so one number can mirror onto multiple leads (same patient,
# two programs) without DuplicateEntryError on refresh / inbound.
tatva_connect.patches.recreate_whatsapp_message_id_index_composite
# Activity/location rebuild: drop the superseded CRM Lead "Location Captures" section + HTML field
# (collapsed into the unified "Activity & Location" view). Idempotent; also runs on after_migrate.
tatva_connect.patches.retire_location_captures_fields
# Lead stage native: drop the redundant lifecycle fields (custom_program_lifecycle_stage,
# custom_lsq_lead_stage_legacy) superseded by the grain-scoped custom_stage. Idempotent;
# also runs on after_migrate.
tatva_connect.patches.retire_lead_stage_legacy_fields
# Orphan cleanup: move WhatsApp template caps onto CRM WATI Settings and delete the misnamed
# `Tatva Automation Settings` single (lost its only switch in the automation-seam refactor).
# Copy-forward preserves tuned caps; idempotent (no-op once gone / on a fresh install).
tatva_connect.patches.retire_tatva_automation_settings
# Automation registry rekey: rename CRM Tatva Automation rows from flat keys (wati, acefone, …)
# to grain-scoped composite `::` keys (WhatsApp::WATI::messaging, …). Preserves operators'
# Enabled state across the PK rename. Idempotent; no-op on a fresh install.
tatva_connect.patches.migrate_automation_to_composite_keys

# ──────────────────────────────────────────────────────────────────────────────
# ARCHIVED — DO NOT RUN. One-time legacy migrations, moved to archive/legacy-patches/
# (gitignored, on disk for bookkeeping). Each "skips if old absent", so they no-op on a
# clean install. Kept here commented for file-level history. To restore for an IN-PLACE
# upgrade of an old-schema prod DB: move the file back into tatva_connect/patches/ and
# uncomment the line (renames belong in [pre_model_sync], the rest in [post_model_sync]).
# tatva_connect.patches.rename_doctypes_partner_api      # pre  — Lead API * -> CRM Lead API *
# tatva_connect.patches.rename_doctypes_whatsapp         # pre  — WATI * -> CRM WATI *
# tatva_connect.patches.rename_doctypes_telephony        # pre  — Acefone * -> CRM Acefone *
# tatva_connect.patches.converge_entity_masters          # post — rekey Doctor/Hospital/City
# tatva_connect.patches.normalize_lead_phones            # post — normalize existing lead phones
# tatva_connect.patches.canonicalize_lead_routing        # post — canonicalize existing routing
# tatva_connect.patches.split_plan_profile               # post — P9: Plan Profile -> child sections
# tatva_connect.patches.rename_to_naming_standard        # post — hash rows -> composite :: keys
# tatva_connect.patches.fix_group_layout_slot            # post — repoint group layout slot
# tatva_connect.patches.fix_custom_group_field_name      # post — custom_psp_group -> custom_group
