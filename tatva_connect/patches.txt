[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations
# Register modules.txt -> Module Def FIRST, before any doctype sync (migrate on an
# existing site doesn't; add_module_defs runs only at install-app). Idempotent.
tatva_connect.patches.register_modules
# The `Push Notifications` module collapsed into the unified `Notifications` module (push is
# one transport, not a domain — mirrors whatsapp/). Repoint the Module Def + its existing
# doctypes on an existing site BEFORE its doctypes sync. Idempotent; no-op on fresh install.
tatva_connect.patches.rename_push_module_to_notifications
# Same story for the Observability module (CRM API Request Log / Metric / Metric Settings):
# register its Module Def before its doctypes sync on an existing site. Idempotent.
tatva_connect.patches.register_observability_module
# Provider-neutral WhatsApp spine: rename CRM WATI Account Routing -> CRM WhatsApp Routing and
# CRM WATI Settings -> CRM WhatsApp Settings, and replace custom_is_wati with the custom_provider
# Select (data preserved). Runs before model sync so the new names/field exist when JSON syncs.
tatva_connect.patches.generalize_whatsapp_spine
# Provider-neutral telephony spine: rename CRM Acefone Account/Routing/Settings -> CRM Telephony
# Account/Routing/Settings, rename the acefone_account / custom_acefone_account fields, and seed
# the provider Select. Data preserved; runs before model sync so the new names exist when JSON syncs.
tatva_connect.patches.generalize_telephony_spine
# Neutralise the WhatsApp Message provider-id field: custom_wati_id -> custom_provider_message_id
# (shared doctype; a neutral name lets a future provider reuse it). Data preserved; idempotent.
tatva_connect.patches.neutralize_wati_message_id_field
# Activity engine 9-column collapse: rename CRM Task Type Field.first_class_target -> target before
# the doctype JSON syncs, so the renamed field map carries existing values (no orphan column).
# Idempotent; no-op on a fresh install. See docs/plans/2026-06-21-activity-engine-unified-design.md.
tatva_connect.patches.rename_task_field_target
# CRM Lead API Field drops the redundant `child_doctype` column (target_doctype is now the single
# source). Carry existing child rows' child_doctype onto target_doctype BEFORE the JSON syncs and
# drops the column, so the smartview composer keeps its child join target. MUST be pre_model_sync.
# Idempotent; guarded no-op once the column is gone / on a fresh install.
tatva_connect.patches.backfill_catalog_target_doctype
# Intake mapping contract: split the legacy free-text CRM Intake Field Map.target ("plan:nivo_dosage")
# into the structured target_table + target_field pair BEFORE the new JSON drops `target`. Format-only
# (does NOT remap plan->drug; that's Phase-D). Idempotent; no-op once the legacy column is gone / fresh.
tatva_connect.patches.migrate_intake_field_map_target

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
tatva_connect.patches.add_whatsapp_message_id_unique_index
tatva_connect.patches.add_acefone_telephony_medium
tatva_connect.patches.seed_india_cities
# B5: shared-number fix — replace the global message_id unique index with a composite
# (message_id, reference_name) so one number can mirror onto multiple leads (same patient,
# two programs) without DuplicateEntryError on refresh / inbound.
tatva_connect.patches.recreate_whatsapp_message_id_index_composite
# Activity/location rebuild: drop the superseded CRM Lead "Location Captures" section + HTML field
# (collapsed into the unified "Activity & Location" view). Idempotent; also runs on after_migrate.
tatva_connect.patches.retire_location_captures_fields
# Lead stage native: drop the redundant lifecycle fields (custom_program_lifecycle_stage,
# custom_lsq_lead_stage_legacy) superseded by the grain-scoped custom_stage. Idempotent;
# also runs on after_migrate.
tatva_connect.patches.retire_lead_stage_legacy_fields
# Orphan cleanup: move WhatsApp template caps onto CRM WATI Settings and delete the misnamed
# `Tatva Automation Settings` single (lost its only switch in the automation-seam refactor).
# Copy-forward preserves tuned caps; idempotent (no-op once gone / on a fresh install).
tatva_connect.patches.retire_tatva_automation_settings
# Automation registry rekey: rename CRM Tatva Automation rows from flat keys (wati, acefone, …)
# to grain-scoped composite `::` keys (WhatsApp::WATI::messaging, …). Preserves operators'
# Enabled state across the PK rename. Idempotent; no-op on a fresh install.
tatva_connect.patches.migrate_automation_to_composite_keys
# Observability: composite indexes on CRM API Metric (granularity+bucket_start,
# granularity+endpoint+bucket_start) — can't be declared in doctype JSON. Idempotent;
# also runs on after_migrate via schema_setup.
tatva_connect.patches.add_observability_indexes
# Composite index on CRM Task (reference_docname, custom_task_type, status) — backs the
# activity-metrics recompute count. Can't be declared in doctype JSON. Idempotent; also runs
# on after_migrate via schema_setup. See docs/plans/2026-06-26-lead-data-model-MASTER-build.md §7.
tatva_connect.patches.add_crm_task_metrics_index
# Fold the legacy task-transition engine into the unified automation engine: the
# `CRM Task Type Transition` child doctype + the `transitions` field on CRM Task Type +
# activity.automation.apply_transitions are removed (doctype archived to archive/doctype/).
# Existing transition rows are migrated to CRM Automation Rule by the operator-run db-seeds SQL
# (db-seeds/2026-06-18-*-migrate-transitions-to-automation-rules.sql) BEFORE this deploy. This
# patch only carries the legacy master switch (Task::CRM Task::transitions) onto the unified one
# (Task::Automation::rules) so on stays on / off stays off; the orphaned legacy registry row is
# then pruned by seed.sync_catalog on after_migrate.
tatva_connect.patches.fold_transitions_switch
# Webhook secrets Data->Password: the inbound webhook tokens (WhatsApp Account.custom_webhook_token,
# CRM Telephony Account.webhook_token) flip to Password. Carry each existing plaintext into the
# encrypted store so values survive the field-type switch (merge-not-clobber; column placeholder is
# the idempotency guard). Idempotent; also runs on after_migrate via schema_setup.
tatva_connect.patches.migrate_webhook_tokens_to_password
# Activity engine 9-column collapse: custom_visit_status -> custom_outcome, custom_next_visit_at ->
# custom_followup_at, custom_demo_scheduled_at -> custom_scheduled_at (values carried forward), the
# old single custom_key_date -> custom_key_date_1..4, custom_reference added — all via fixtures. This
# patch drops the stale columns and the 4 dead archetype child doctypes (CRM Task Order/Clinical/Call/
# Doc Detail — defined, never written; folders archived to archive/doctype/). Idempotent; also runs on
# after_migrate via schema_setup. See docs/plans/2026-06-21-activity-engine-unified-design.md.
tatva_connect.patches.retire_activity_legacy_columns
# Nivolumab mapping fix (Phase D): the live Nivolumab CRM Intake Field Map rows map
# nivo_dosage / nivo_indication to target_table='plan', but those fields live on the `drug`
# child (custom_drug_program_profile) — silently dropped under the old fold, and now FAIL the
# Phase-B validate(). Repoint those rows' target_table 'plan' -> 'drug'. Data-only: does NOT
# repoint the live Web Form's doc_type and does NOT drop any submission table (the public URL
# keeps working; the full re-platform is a manual runbook step). Idempotent; no-op once fixed /
# on a fresh install. See docs/plans/intake-form-builder-runbook.md.
tatva_connect.patches.fix_nivolumab_drug_mapping
# L3 trace (Invariant 14): the dead `debug_log_payloads` Check + its `section_break_debug`
# Section Break were removed from CRM WhatsApp Settings JSON — never wired into the spine (the
# spine raw-logs always-on), zero readers. No patch needed: an unused single-doctype field
# orphans harmlessly; field sync drops it on the next migrate. Noted here for file-level history.

# ──────────────────────────────────────────────────────────────────────────────
# ARCHIVED — DO NOT RUN. One-time legacy migrations, moved to archive/legacy-patches/
# (gitignored, on disk for bookkeeping). Each "skips if old absent", so they no-op on a
# clean install. Kept here commented for file-level history. To restore for an IN-PLACE
# upgrade of an old-schema prod DB: move the file back into tatva_connect/patches/ and
# uncomment the line (renames belong in [pre_model_sync], the rest in [post_model_sync]).
# tatva_connect.patches.register_push_notifications_module  # pre  — superseded by rename_push_module_to_notifications
# tatva_connect.patches.rename_doctypes_partner_api      # pre  — Lead API * -> CRM Lead API *
# tatva_connect.patches.rename_doctypes_whatsapp         # pre  — WATI * -> CRM WATI *
# tatva_connect.patches.rename_doctypes_telephony        # pre  — Acefone * -> CRM Acefone *
# tatva_connect.patches.converge_entity_masters          # post — rekey Doctor/Hospital/City
# tatva_connect.patches.normalize_lead_phones            # post — normalize existing lead phones
# tatva_connect.patches.canonicalize_lead_routing        # post — canonicalize existing routing
# tatva_connect.patches.split_plan_profile               # post — P9: Plan Profile -> child sections
# tatva_connect.patches.rename_to_naming_standard        # post — hash rows -> composite :: keys
# tatva_connect.patches.fix_group_layout_slot            # post — repoint group layout slot
# tatva_connect.patches.fix_custom_group_field_name      # post — custom_psp_group -> custom_group
